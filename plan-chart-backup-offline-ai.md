# برنامهٔ اجرایی پشتیبان‌گیری، مدل آفلاین و نمودار تاریخی

مبنا: بررسی کد محلی در commit `d526442`، ۱۳ سپتامبر ۲۰۲۶. این سند فقط برنامهٔ کار است؛ هیچ دادهٔ جدید، مدل یا اصلاح عملکردی با نوشتن آن اضافه نشده است. چهار نماد این برنامه `XAUUSD`، `EURUSD`، `GBPUSD` و `USDJPY` هستند. اجرای سفارش واقعی خارج از دامنه است.

## یافته‌های فعلی و علت‌های قابل‌ردیابی

### پشتیبان‌گیری

- `components/trading/export-import-modal.tsx` یک JSON از فقط `symbol`، `currentStepIndex`، `accountBalance` و `accountEquity` می‌سازد. متن رابط وعدهٔ «تنظیمات شبیه‌ساز» می‌دهد، اما تنظیمات، چارت، معاملات، ژورنال، داده‌های واردشده و مدل‌های AI در فایل نیستند.
- `lib/persistence/storage.ts` هنگام import فقط `XAUUSD` و `EURUSD` را می‌پذیرد؛ در حالی که `app/page.tsx` چهار نماد را بازیابی می‌کند. بنابراین پشتیبان `GBPUSD` و `USDJPY` با validator فعلی رد می‌شود.
- `app/page.tsx::handleStateRestored` فقط نماد و گام ریپلی را اعمال می‌کند؛ موجودی و اکوئیتی داخل فایل دوباره به broker برگردانده نمی‌شوند. پیام «بازیابی موفق» در این حالت گمراه‌کننده است.
- دکمهٔ Export بلافاصله پس از `a.click()` پیام موفقیت می‌دهد؛ پایان واقعی دانلود یا قابل‌بازیابی بودن فایل را بررسی نمی‌کند. ورودی `FileReader` نیز خطای خواندن و تغییر مجدد همان فایل را به‌روشنی مدیریت نمی‌کند.
- `components/trading/security-dr-panel.tsx` و `app/api/recovery/route.ts` یک snapshot جداگانهٔ OMS/ژورنال دارند. این snapshot جایگزین پشتیبان محلی ریپلی نیست؛ مرز، دامنه و شیوهٔ بازیابی آن‌ها باید در UI روشن شود. هیچ snapshot سروری بدون بررسی احراز هویت و دادهٔ حساس برای دانلود عمومی عرضه نشود.

### دانلود مدل‌های AI آفلاین

- `lib/ai/browser-offline-ai.ts` قبل از بارگذاری، وجود model ID در registry نصب‌شدهٔ WebLLM و دسترسی WebGPU را بررسی می‌کند. مدل‌های خارج از registry و `Chrome-Builtin` در این نسخه دانلودشدنی نیستند؛ صرف حضور در `PLAN_V4_MODELS` به معنی قابل‌دانلود بودن نیست.
- مسیر `hf-mirror.com` به‌طور پیش‌فرض فعال است و URLهای registry را در زمان اجرا بازنویسی می‌کند. دسترسی نداشتن به mirror، CORS، خطای وزن/wasm، کمبود quota یا کمبود حافظهٔ GPU می‌تواند بارگذاری را متوقف کند. بدون لاگ مرورگر و دستگاه هدف، یکی از این‌ها را علت قطعی تجربهٔ کاربر اعلام نکنیم.
- `loadModelToMemory()` دانلود، cache، ساخت Worker و بارگذاری GPU را در یک عملیات انجام می‌دهد؛ رابط تفاوت «دانلود شد»، «در cache است»، «در GPU resident است» و «آفلاین واقعاً آزموده شد» را باید دقیق نگه دارد. اعداد MB در progress از درصد و حجم تخمینی محاسبه می‌شوند، نه از بایت واقعی دانلودشده.
- اگر Worker خطا بدهد، مسیر اصلی `CreateMLCEngine` امتحان می‌شود؛ نوع و مرحلهٔ خطا باید در رابط قابل مشاهده باشد. تست داخلی یا build موفق، دانلود واقعی روی گوشی/مرورگر کاربر را اثبات نمی‌کند.

### نمودار و داده‌ها

- `lib/replay/replay-engine.ts` چارت اولیه را از fixture کوتاه 5M می‌سازد و از حدود گام ۸۰/۹۵ آغاز می‌کند. `app/page.tsx` همین `visibleCandles` ریپلی را به چارت می‌دهد. بنابراین بخش 5M/15M از کل تاریخ موجود استفاده نمی‌کند.
- `components/trading/chart-canvas.tsx` نمایش را با `visibleCandleCount=100` شروع می‌کند. برای 5M/15M فقط انتهای دادهٔ ریپلی، برای 1H/4H حداکثر حدود دو برابر این مقدار، و برای D1 حداکثر ۳۶۵ کندل گرفته می‌شود. چارت دوم نیز سقف ۱۰۰/۱۵۰ کندل دارد. دکمهٔ zoom فقط تعداد کندل انتهایی را عوض می‌کند؛ pan/scroll زمانی، انتخاب بازه و بارگذاری صفحه‌ای تاریخ وجود ندارد.
- چارت فعلی فقط `5M`، `15M`، `1H`، `4H` و `D1` را در انتخابگر اصلی نشان می‌دهد. نوع `Timeframe` شامل `1M` و `W1` است، اما loader فعلی آن‌ها را برای چارت ارائه نمی‌کند.
- manifest فعلی ۲۰۲۴ برای EURUSD/GBPUSD/USDJPY هفت تایم‌فریم 1M تا W1 دارد؛ برای XAUUSD فقط D1/4H/1H ثبت کرده است. فایل‌های ۲۰۲۵ برای چهار نماد در 1M تا D1 موجودند، اما intraday آن‌ها از interpolation کندل روزانه ساخته شده و واقعی نیستند. برای ۲۰۲۵ W1 معتبر و یکپارچه در loader/manifest فعلی وجود ندارد. هیچ‌کدام را بدون منشأ و پوشش واقعی «دادهٔ واقعی کامل» معرفی نکنیم.
- `lib/research/dataset-manifest.ts` در حال حاضر provenance را صرفاً از سال حدس می‌زند؛ `lib/core/yearly-data-loader.ts` مسیر CSV را از الگوی نام می‌سازد. این برای انتخاب دقیق dataset، checksum و تفاوت واقعی/تجمیعی/مصنوعی کافی نیست.

## هدف رفتاری

کاربر برای هر چهار نماد بتواند `1M`، `5M`، `15M`، `1H`، `4H`، `D1` و `W1` را انتخاب کند، بازهٔ زمانی موجود را ببیند، به گذشته pan/scroll کند، zoom کند و از یک تاریخ به تاریخ دیگر برود. نمایش چارت، ریپلی و بک‌تست باید سه حالت مستقل با برچسب روشن باشند. دادهٔ تاریخی فقط وقتی در چارت قابل انتخاب باشد که فایل یا سرویس واقعی، manifest معتبر و آزمون کیفیت داشته باشد؛ بازهٔ فاقد داده با پیام روشن نمایش داده شود.

پشتیبان باید دقیقاً اعلام کند چه چیزهایی را نگه می‌دارد و restore را واقعاً اجرا و تأیید کند. مرکز AI باید قبل از دانلود وضعیت پشتیبانی، WebGPU، فضای cache، منبع و حجم تخمینی را نشان دهد و پس از آن وضعیت cache و آزمون آفلاین را جدا گزارش کند.

## مراحل اجرا

- [ ] Step 1: روی یک مرورگر و دستگاه مشخص، سه سناریوی فعلی را بازتولید و ثبت کن: Export/Import برای هر چهار نماد، دانلود سبک‌ترین مدل واقعاً پشتیبانی‌شده، و حرکت/zoom چارت در هر تایم‌فریم. Network/console/WebGPU/quota، وضعیت واقعی دانلود و متن خطا را ثبت کن. این مرحله علت دستگاهی دانلود را از علت کدی جدا می‌کند.
- [ ] Step 2: قرارداد نسخه‌دار `BackupV2` و فهرست دقیق اقلام قابل‌پشتیبان را تعریف کن. پشتیبان ریپلی/تنظیمات محلی را از snapshot حساس سروری و cache وزن مدل جدا کن. برای فایل قدیمی `v1.0` migration یا پیام سازگاری واضح بده.
- [ ] Step 3: validator و restore را اصلاح کن: چهار نماد، اعداد محدود و معتبر، بازهٔ گام متناسب با dataset، نسخه/شناسهٔ dataset، checksum و پیش‌نمایش قبل از اعمال. restore باید state مورد ادعا را به مقصد واقعی برگرداند؛ اگر موجودی/پوزیشن‌ها به‌دلیل ایمنی بازیابی نمی‌شوند، آن‌ها را از وعدهٔ UI و payload عملیاتی حذف کن. آزمون round-trip و فایل خراب/ناسازگار اضافه کن.
- [ ] Step 4: مرکز AI را با preflight واقعی اصلاح کن: registry، WebGPU/adapter، shader features، quota، اتصال به منبع، سازگاری artifact و اندازهٔ تقریبی. مدل unsupported/Chrome-Builtin را به‌جای دکمهٔ دانلود با دلیل روشن غیرفعال کن. mirror را انتخابی و قابل‌سنجش کن؛ خطاها را به مرحلهٔ دقیق fetch/cache/compile/GPU نسبت بده.
- [ ] Step 5: دانلود یک مدل کوچک و پشتیبانی‌شده را در مرورگر هدف از صفر تا `cached`، سپس `resident`، استنتاج واقعی و بعد آزمون با شبکه قطع‌شده تأیید کن. progress بایت واقعی یا عبارت «تخمینی» داشته باشد؛ retry/cancel و cleanup فایل نیمه‌کاره آزموده شود. فقط مدل‌های واقعاً آزموده‌شده در فهرست آماده نمایش داده شوند.
- [ ] Step 6: catalog داده را برای ۴ نماد × ۷ تایم‌فریم تهیه کن. برای هر مورد sourceKind، provider، license، UTC، آغاز/پایان، bars، gaps، SHA-256، منشأ aggregation و محدودیت استفاده را ثبت کن. فایل‌های ۲۰۲۵ مصنوعی را جدا نشان بده؛ دادهٔ معتبر M1 طلا و W1 موارد ناقص را از منبع مجاز تأمین یا از D1/M1 معتبر با روش ثبت‌شده بساز. دادهٔ مصنوعی هرگز جای خالی دادهٔ واقعی را پنهانی پر نکند.
- [ ] Step 7: ingestion/validator را روی کل ماتریس اجرا کن: ترتیب و یکتایی timestamp، OHLC، حجم، تعطیلی بازار، timezone/DST، checksum و سازگاری aggregation با منبع. برای هر dataset وضعیت `READY/PARTIAL/REJECTED` و گزارش کیفیت بساز. قبل از افزودن فایل‌های حجیم به Git، حق بازنشر و راهکار storage/CDN را تعیین کن.
- [ ] Step 8: chart data source مستقل از ReplayEngine بساز. انتخاب symbol/timeframe/year/range از catalog انجام شود؛ کندل‌ها به‌صورت صفحه‌ای/chunk با cache محدود بار شوند. حالت replay فقط تا timestamp فعلی نمایش دهد؛ حالت historical کل دادهٔ مجاز را برای پیمایش در دسترس بگذارد.
- [ ] Step 9: نمودار را به viewport زمانی واقعی مجهز کن: pan با drag/touch، zoom حول مکان نشانگر، scroll به عقب/جلو، پرش به تاریخ، نمایش تعداد کندل visible و کل داده، محور زمان و reset view. فقط کندل‌های داخل viewport با overscan رندر شوند تا M1 سالانه مرورگر را از کار نیندازد. هر هفت تایم‌فریم در هر دو چارت با کنترل‌های موبایل در دسترس باشند.
- [ ] Step 10: بک‌تست را به همین catalog متصل کن تا dataset انتخابی، بازهٔ ورودی و بازهٔ اجراشده یکی و در گزارش ثبت شوند. کنترل style/timeframe و ممنوعیت نتیجه‌گیری edge از دادهٔ مصنوعی را اضافه کن؛ نبود داده خطای صریح بدهد.
- [ ] Step 11: آزمون end-to-end روی desktop و mobile اجرا کن: چهار نماد × هفت تایم‌فریم، دادهٔ ناقص/ناموجود، pan/zoom/پرش تاریخ، سوییچ replay↔historical، خروجی و بازیابی backup، دانلود و اجرای واقعی مدل کوچک، قطع شبکه و بازگشت. typecheck، lint، تست دامنه و build نیز باید موفق شوند.

## مسیرهای اصلی برای مدل پیاده‌ساز

| موضوع | فایل‌های فعلی / فایل پیشنهادی |
|---|---|
| پشتیبان محلی | `lib/persistence/storage.ts`، `components/trading/export-import-modal.tsx`، `app/page.tsx`؛ جدید: `lib/persistence/backup-v2.ts` و تست round-trip |
| snapshot سروری | `components/trading/security-dr-panel.tsx`، `app/api/recovery/route.ts`، `lib/server/disaster-recovery.ts`؛ مرزبندی و حفاظت جداگانه |
| مدل آفلاین | `lib/ai/browser-offline-ai.ts`، `lib/ai/web-llm.worker.ts`، `components/trading/offline-ai-manager-modal.tsx`، `lib/ai/__tests__/offline-ai-safety.test.ts`؛ جدید: diagnostics/preflight و تست مرورگری |
| داده‌ها | `lib/research/dataset-manifest.ts`، `lib/research/dataset-validator.ts`، `lib/research/dataset-catalog.ts`، `lib/core/yearly-data-loader.ts`، `public/historical/intraday/manifest.json`، `scripts/build-bundled-intraday-datasets.ts`؛ جدید: catalog و quality report کامل چهار نماد |
| ریپلی و چارت | `lib/replay/replay-engine.ts`، `components/trading/chart-canvas.tsx`، `app/page.tsx`؛ جدید: `lib/market/chart-data-source.ts`، `hooks/use-chart-history.ts` و تست pan/zoom |
| بک‌تست | `components/trading/multi-style-backtest-modal.tsx`، `lib/core/backtest.worker.ts`، `lib/contracts/backtester.ts`؛ گزارش بازه و منشأ |

## ریسک‌ها و معیار پذیرش

- ۲۸ انتخاب تایم‌فریم به معنی ۲۸ فایل واقعی کامل نیست. پوشش و کیفیت هر مورد جداگانه گزارش شود. تأمین تاریخچهٔ M1 هر چهار نماد ممکن است به مجوز داده، هزینه و فضای ذخیره‌سازی نیاز داشته باشد.
- browser cache مدل جزو backup JSON نیست و تضمین ماندگاری پس از پاک‌سازی دادهٔ مرورگر ندارد. آفلاین فقط پس از دانلود کامل، cache معتبر و آزمون واقعی بدون شبکه تأیید شود.
- restore دادهٔ حساس سروری نباید از فایل محلی بدون احراز هویت، پیش‌نمایش و کنترل تکرار سفارش انجام شود.
- رندر همهٔ کندل‌های M1 در یک DOM/SVG ممکن است کند یا ناممکن شود؛ معیار موفقیت، امکان پیمایش کل تاریخ با رندر محدود به viewport است، نه نمایش هم‌زمان همهٔ نقاط روی یک صفحه.
- پذیرش نهایی: backup چهار نماد round-trip کند؛ مدل سبک روی دستگاه هدف واقعاً دانلود، اجرا و آفلاین آزموده شود؛ چارت هر چهار نماد و هفت تایم‌فریم را با بازهٔ واقعی، pan و zoom نشان دهد؛ هیچ دادهٔ synthetic یا replay با عنوان historical واقعی ارائه نشود.
